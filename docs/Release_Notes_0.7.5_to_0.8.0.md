# CleanupSuite Release Notes - 0.7.5 to 0.8.0

**Release:** 0.8.0
**Date:** 2026-06-19

## Meaning

CleanupSuite 0.8.0 is the current working release for the `0.8.x` line.

This release moves the suite forward from the documented `0.7.5` release into the redesigned guided-form work and release-polish pass that now defines the current `0.8.0` state.

## Added

- Human-friendly user manual Markdown for `0.8.0`.
- Programmer's guide for adding tools for `0.8.0`.
- Refined tool reference for `0.8.0`.
- Human-friendly user manual PDF for `0.8.0`.
- DOCX exports for the `0.8.0` user manual, programmer guide, and tool reference.
- Current release package notes for the `0.8.0` documentation set.
- This release-notes file and a matching `0.8.0` documentation handoff note.

## Changed

- Updated project version metadata to `0.8.0`.
- Updated current-facing documentation wording so `0.8.0` is described directly as the current release state instead of as a carried-forward `0.7.5` baseline.
- Updated the user-manual PDF link/versioned artifact to the `v0.8.0` release path.
- Continued the guided tool-form redesign and layout cleanup that now represents the current `0.8.0` behavior.

## Not Yet Included

- Additional `0.8.0` PDF-style export artifacts beyond the current user-manual PDF already checked in.

## Verification Note

The checked-in source and generated bundle metadata identify the suite as `0.8.0`, and the current documentation package includes the `0.8.0` user manual Markdown plus the `0.8.0` PDF artifact.
